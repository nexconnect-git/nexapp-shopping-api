upstream backend {
    server backend:8000;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 100M;
    charset utf-8;

    # Nginx logging
    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log;

    # Customer App (Default)
    location /sa/ {
        alias /usr/share/nginx/html/customer/;
        try_files $uri $uri/ /sa/index.html;
    }

    # Admin Panel
    location /sa/admin/ {
        alias /usr/share/nginx/html/admin/;
        try_files $uri $uri/ /sa/admin/index.html;
    }

    # Vendor App
    location /sa/vendor/ {
        alias /usr/share/nginx/html/vendor/;
        try_files $uri $uri/ /sa/vendor/index.html;
    }

    # Delivery App
    location /sa/delivery/ {
        alias /usr/share/nginx/html/delivery/;
        try_files $uri $uri/ /sa/delivery/index.html;
    }

    # Django Media
    location /sa/media/ {
        alias /app/media/;
    }

    # Django Static
    location /sa/django_static/ {
        alias /app/staticfiles/;
    }

    # Django API (Backend)
    location /sa/api/ {
        rewrite ^/sa/api/(.*) /api/$1 break;
        proxy_pass http://backend;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }

    # WebSocket Support (Daphne/Channels)
    location /sa/ws/ {
        rewrite ^/sa/ws/(.*) /ws/$1 break;
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
